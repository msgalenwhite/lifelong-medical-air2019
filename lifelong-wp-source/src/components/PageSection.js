const React = require('React');

const PageSection = props => {

  return(
    <div>
      {/* picture on one side */}
      {/* Other side:
          - header (centered)
          centered div
          left justify text
         */}
         {/* param to alternate sides */}
    </div>
  )
}

module.exports = PageSection;
